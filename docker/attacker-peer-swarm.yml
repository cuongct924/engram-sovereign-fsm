name: engram-attacker-swarm

# Live Sybil/slot-exhaustion attacker swarm for E4/E8's A1 (Peer Slot
# Exhaustion) and A2 (Sybil via multi-subnet swarm) -- tests the real
# ingress filter (FilterPeerByAddr, Params.MaxPeersPerSubnet) with real
# traffic, not synthetic simulation.
#
# Every attacker* is a real `engramd` full-node process (same image as the
# validators, same P2P handshake) but outside the genesis validator set --
# pure P2P participants dialing targets aggressively. BTC/DA sensor env
# vars omitted: wireBTCSensor/wireDASensor no-op cleanly when unset.
#
# Profile-gated (attacker-swarm-a1/a2) -- only runs when the profile is
# explicitly requested.
#
# Requires the real cluster's shared genesis (../testnet-data/engram-node01/
# config/genesis.json, `engramd testnet init-files --v 4`) -- bind-mounted
# read-only so each attacker matches the cluster's chain-id/network.

x-attacker-common: &attacker-common
  build:
    context: ..
    dockerfile: Dockerfile
    additional_contexts:
      - cometbft-fork=../../engram-consensus-core
  restart: "no"
  # Run `engramd init` first (idempotent; genesis already bind-mounted),
  # then dial the target and start.
  entrypoint: ["/bin/sh", "-c"]

services:
  # ===========================================================
  # A1 leg -- Peer Slot Exhaustion: all attackers on engram-net dialing
  # node01, tripping MaxPeersPerSubnet (8) before CometBFT's own
  # MaxNumInboundPeers (40) -- the filter is the binding constraint.
  # ===========================================================
  attacker-a1-01: &attacker-a1
    <<: *attacker-common
    hostname: attacker-a1-01
    container_name: attacker-a1-01
    profiles: [attacker-swarm-a1]
    networks:
      engram-net:
        ipv4_address: 172.28.0.201
    volumes:
      - ../testnet-data/attacker-a1-01:/root/.engramd
      - ../testnet-data/engram-node01/config/genesis.json:/root/.engramd/config/genesis.json:ro
    environment:
      - TARGET_PEER=engram-node01:26656
    command:
      - |
        engramd init attacker-a1-01 --home /root/.engramd || true
        NODE_ID=$$(engramd tendermint show-node-id --home /root/.engramd 2>/dev/null || true)
        echo "attacker-a1-01 node_id=$$NODE_ID dialing $$TARGET_PEER"
        TARGET_ID=$$(curl -s "http://$${TARGET_PEER%%:*}:26657/status" | jq -r .result.node_info.id)
        sed -i "s#^persistent_peers *=.*#persistent_peers = \"$${TARGET_ID}@$$TARGET_PEER\"#" /root/.engramd/config/config.toml
        sed -i "s#^addr_book_strict *=.*#addr_book_strict = false#" /root/.engramd/config/config.toml
        sed -i "s#^allow_duplicate_ip *=.*#allow_duplicate_ip = true#" /root/.engramd/config/config.toml
        exec engramd start --home /root/.engramd

  attacker-a1-02:
    <<: *attacker-a1
    hostname: attacker-a1-02
    container_name: attacker-a1-02
    networks:
      engram-net:
        ipv4_address: 172.28.0.202
    volumes:
      - ../testnet-data/attacker-a1-02:/root/.engramd
      - ../testnet-data/engram-node01/config/genesis.json:/root/.engramd/config/genesis.json:ro
    command:
      - |
        engramd init attacker-a1-02 --home /root/.engramd || true
        TARGET_ID=$$(curl -s "http://$${TARGET_PEER%%:*}:26657/status" | jq -r .result.node_info.id)
        sed -i "s#^persistent_peers *=.*#persistent_peers = \"$${TARGET_ID}@$$TARGET_PEER\"#" /root/.engramd/config/config.toml
        sed -i "s#^addr_book_strict *=.*#addr_book_strict = false#" /root/.engramd/config/config.toml
        sed -i "s#^allow_duplicate_ip *=.*#allow_duplicate_ip = true#" /root/.engramd/config/config.toml
        exec engramd start --home /root/.engramd

  attacker-a1-03:
    <<: *attacker-a1
    hostname: attacker-a1-03
    container_name: attacker-a1-03
    networks:
      engram-net:
        ipv4_address: 172.28.0.203
    volumes:
      - ../testnet-data/attacker-a1-03:/root/.engramd
      - ../testnet-data/engram-node01/config/genesis.json:/root/.engramd/config/genesis.json:ro
    command:
      - |
        engramd init attacker-a1-03 --home /root/.engramd || true
        TARGET_ID=$$(curl -s "http://$${TARGET_PEER%%:*}:26657/status" | jq -r .result.node_info.id)
        sed -i "s#^persistent_peers *=.*#persistent_peers = \"$${TARGET_ID}@$$TARGET_PEER\"#" /root/.engramd/config/config.toml
        sed -i "s#^addr_book_strict *=.*#addr_book_strict = false#" /root/.engramd/config/config.toml
        sed -i "s#^allow_duplicate_ip *=.*#allow_duplicate_ip = true#" /root/.engramd/config/config.toml
        exec engramd start --home /root/.engramd

  attacker-a1-04:
    <<: *attacker-a1
    hostname: attacker-a1-04
    container_name: attacker-a1-04
    networks:
      engram-net:
        ipv4_address: 172.28.0.204
    volumes:
      - ../testnet-data/attacker-a1-04:/root/.engramd
      - ../testnet-data/engram-node01/config/genesis.json:/root/.engramd/config/genesis.json:ro
    command:
      - |
        engramd init attacker-a1-04 --home /root/.engramd || true
        TARGET_ID=$$(curl -s "http://$${TARGET_PEER%%:*}:26657/status" | jq -r .result.node_info.id)
        sed -i "s#^persistent_peers *=.*#persistent_peers = \"$${TARGET_ID}@$$TARGET_PEER\"#" /root/.engramd/config/config.toml
        sed -i "s#^addr_book_strict *=.*#addr_book_strict = false#" /root/.engramd/config/config.toml
        sed -i "s#^allow_duplicate_ip *=.*#allow_duplicate_ip = true#" /root/.engramd/config/config.toml
        exec engramd start --home /root/.engramd

  attacker-a1-05:
    <<: *attacker-a1
    hostname: attacker-a1-05
    container_name: attacker-a1-05
    networks:
      engram-net:
        ipv4_address: 172.28.0.205
    volumes:
      - ../testnet-data/attacker-a1-05:/root/.engramd
      - ../testnet-data/engram-node01/config/genesis.json:/root/.engramd/config/genesis.json:ro
    command:
      - |
        engramd init attacker-a1-05 --home /root/.engramd || true
        TARGET_ID=$$(curl -s "http://$${TARGET_PEER%%:*}:26657/status" | jq -r .result.node_info.id)
        sed -i "s#^persistent_peers *=.*#persistent_peers = \"$${TARGET_ID}@$$TARGET_PEER\"#" /root/.engramd/config/config.toml
        sed -i "s#^addr_book_strict *=.*#addr_book_strict = false#" /root/.engramd/config/config.toml
        sed -i "s#^allow_duplicate_ip *=.*#allow_duplicate_ip = true#" /root/.engramd/config/config.toml
        exec engramd start --home /root/.engramd

  attacker-a1-06:
    <<: *attacker-a1
    hostname: attacker-a1-06
    container_name: attacker-a1-06
    networks:
      engram-net:
        ipv4_address: 172.28.0.206
    volumes:
      - ../testnet-data/attacker-a1-06:/root/.engramd
      - ../testnet-data/engram-node01/config/genesis.json:/root/.engramd/config/genesis.json:ro
    command:
      - |
        engramd init attacker-a1-06 --home /root/.engramd || true
        TARGET_ID=$$(curl -s "http://$${TARGET_PEER%%:*}:26657/status" | jq -r .result.node_info.id)
        sed -i "s#^persistent_peers *=.*#persistent_peers = \"$${TARGET_ID}@$$TARGET_PEER\"#" /root/.engramd/config/config.toml
        sed -i "s#^addr_book_strict *=.*#addr_book_strict = false#" /root/.engramd/config/config.toml
        sed -i "s#^allow_duplicate_ip *=.*#allow_duplicate_ip = true#" /root/.engramd/config/config.toml
        exec engramd start --home /root/.engramd

  attacker-a1-07:
    <<: *attacker-a1
    hostname: attacker-a1-07
    container_name: attacker-a1-07
    networks:
      engram-net:
        ipv4_address: 172.28.0.207
    volumes:
      - ../testnet-data/attacker-a1-07:/root/.engramd
      - ../testnet-data/engram-node01/config/genesis.json:/root/.engramd/config/genesis.json:ro
    command:
      - |
        engramd init attacker-a1-07 --home /root/.engramd || true
        TARGET_ID=$$(curl -s "http://$${TARGET_PEER%%:*}:26657/status" | jq -r .result.node_info.id)
        sed -i "s#^persistent_peers *=.*#persistent_peers = \"$${TARGET_ID}@$$TARGET_PEER\"#" /root/.engramd/config/config.toml
        sed -i "s#^addr_book_strict *=.*#addr_book_strict = false#" /root/.engramd/config/config.toml
        sed -i "s#^allow_duplicate_ip *=.*#allow_duplicate_ip = true#" /root/.engramd/config/config.toml
        exec engramd start --home /root/.engramd

  attacker-a1-08:
    <<: *attacker-a1
    hostname: attacker-a1-08
    container_name: attacker-a1-08
    networks:
      engram-net:
        ipv4_address: 172.28.0.208
    volumes:
      - ../testnet-data/attacker-a1-08:/root/.engramd
      - ../testnet-data/engram-node01/config/genesis.json:/root/.engramd/config/genesis.json:ro
    command:
      - |
        engramd init attacker-a1-08 --home /root/.engramd || true
        TARGET_ID=$$(curl -s "http://$${TARGET_PEER%%:*}:26657/status" | jq -r .result.node_info.id)
        sed -i "s#^persistent_peers *=.*#persistent_peers = \"$${TARGET_ID}@$$TARGET_PEER\"#" /root/.engramd/config/config.toml
        sed -i "s#^addr_book_strict *=.*#addr_book_strict = false#" /root/.engramd/config/config.toml
        sed -i "s#^allow_duplicate_ip *=.*#allow_duplicate_ip = true#" /root/.engramd/config/config.toml
        exec engramd start --home /root/.engramd

  attacker-a1-09:
    <<: *attacker-a1
    hostname: attacker-a1-09
    container_name: attacker-a1-09
    networks:
      engram-net:
        ipv4_address: 172.28.0.209
    volumes:
      - ../testnet-data/attacker-a1-09:/root/.engramd
      - ../testnet-data/engram-node01/config/genesis.json:/root/.engramd/config/genesis.json:ro
    command:
      - |
        engramd init attacker-a1-09 --home /root/.engramd || true
        TARGET_ID=$$(curl -s "http://$${TARGET_PEER%%:*}:26657/status" | jq -r .result.node_info.id)
        sed -i "s#^persistent_peers *=.*#persistent_peers = \"$${TARGET_ID}@$$TARGET_PEER\"#" /root/.engramd/config/config.toml
        sed -i "s#^addr_book_strict *=.*#addr_book_strict = false#" /root/.engramd/config/config.toml
        sed -i "s#^allow_duplicate_ip *=.*#allow_duplicate_ip = true#" /root/.engramd/config/config.toml
        exec engramd start --home /root/.engramd

  attacker-a1-10:
    <<: *attacker-a1
    hostname: attacker-a1-10
    container_name: attacker-a1-10
    networks:
      engram-net:
        ipv4_address: 172.28.0.210
    volumes:
      - ../testnet-data/attacker-a1-10:/root/.engramd
      - ../testnet-data/engram-node01/config/genesis.json:/root/.engramd/config/genesis.json:ro
    command:
      - |
        engramd init attacker-a1-10 --home /root/.engramd || true
        TARGET_ID=$$(curl -s "http://$${TARGET_PEER%%:*}:26657/status" | jq -r .result.node_info.id)
        sed -i "s#^persistent_peers *=.*#persistent_peers = \"$${TARGET_ID}@$$TARGET_PEER\"#" /root/.engramd/config/config.toml
        sed -i "s#^addr_book_strict *=.*#addr_book_strict = false#" /root/.engramd/config/config.toml
        sed -i "s#^allow_duplicate_ip *=.*#allow_duplicate_ip = true#" /root/.engramd/config/config.toml
        exec engramd start --home /root/.engramd

  # ===========================================================
  # A2 leg -- Sybil via multi-subnet swarm: 3 attackers x 4 subnets = 12,
  # spread across attacker-subnet-a/b/c/d so SubnetOf sees genuine
  # diversity -- the buildable analog of A2 (BGP hijacking is out of scope).
  # ===========================================================
  attacker-a2-a1: &attacker-a2
    <<: *attacker-common
    hostname: attacker-a2-a1
    container_name: attacker-a2-a1
    profiles: [attacker-swarm-a2]
    networks:
      attacker-subnet-a:
        ipv4_address: 172.30.0.11
        gw_priority: 1
      engram-net:
        gw_priority: 0
    volumes:
      - ../testnet-data/attacker-a2-a1:/root/.engramd
      - ../testnet-data/engram-node01/config/genesis.json:/root/.engramd/config/genesis.json:ro
    environment:
      - TARGET_PEER=engram-node01:26656
    command:
      - |
        engramd init attacker-a2-a1 --home /root/.engramd || true
        TARGET_ID=$$(curl -s "http://$${TARGET_PEER%%:*}:26657/status" | jq -r .result.node_info.id)
        sed -i "s#^persistent_peers *=.*#persistent_peers = \"$${TARGET_ID}@$$TARGET_PEER\"#" /root/.engramd/config/config.toml
        sed -i "s#^addr_book_strict *=.*#addr_book_strict = false#" /root/.engramd/config/config.toml
        sed -i "s#^allow_duplicate_ip *=.*#allow_duplicate_ip = true#" /root/.engramd/config/config.toml
        exec engramd start --home /root/.engramd

  attacker-a2-a2:
    <<: *attacker-a2
    hostname: attacker-a2-a2
    container_name: attacker-a2-a2
    networks:
      attacker-subnet-a:
        ipv4_address: 172.30.0.12
      engram-net: {}
    volumes:
      - ../testnet-data/attacker-a2-a2:/root/.engramd
      - ../testnet-data/engram-node01/config/genesis.json:/root/.engramd/config/genesis.json:ro
    command:
      - |
        engramd init attacker-a2-a2 --home /root/.engramd || true
        TARGET_ID=$$(curl -s "http://$${TARGET_PEER%%:*}:26657/status" | jq -r .result.node_info.id)
        sed -i "s#^persistent_peers *=.*#persistent_peers = \"$${TARGET_ID}@$$TARGET_PEER\"#" /root/.engramd/config/config.toml
        sed -i "s#^addr_book_strict *=.*#addr_book_strict = false#" /root/.engramd/config/config.toml
        sed -i "s#^allow_duplicate_ip *=.*#allow_duplicate_ip = true#" /root/.engramd/config/config.toml
        exec engramd start --home /root/.engramd

  attacker-a2-a3:
    <<: *attacker-a2
    hostname: attacker-a2-a3
    container_name: attacker-a2-a3
    networks:
      attacker-subnet-a:
        ipv4_address: 172.30.0.13
      engram-net: {}
    volumes:
      - ../testnet-data/attacker-a2-a3:/root/.engramd
      - ../testnet-data/engram-node01/config/genesis.json:/root/.engramd/config/genesis.json:ro
    command:
      - |
        engramd init attacker-a2-a3 --home /root/.engramd || true
        TARGET_ID=$$(curl -s "http://$${TARGET_PEER%%:*}:26657/status" | jq -r .result.node_info.id)
        sed -i "s#^persistent_peers *=.*#persistent_peers = \"$${TARGET_ID}@$$TARGET_PEER\"#" /root/.engramd/config/config.toml
        sed -i "s#^addr_book_strict *=.*#addr_book_strict = false#" /root/.engramd/config/config.toml
        sed -i "s#^allow_duplicate_ip *=.*#allow_duplicate_ip = true#" /root/.engramd/config/config.toml
        exec engramd start --home /root/.engramd

  attacker-a2-b1:
    <<: *attacker-a2
    hostname: attacker-a2-b1
    container_name: attacker-a2-b1
    networks:
      attacker-subnet-b:
        ipv4_address: 172.31.0.11
      engram-net: {}
    volumes:
      - ../testnet-data/attacker-a2-b1:/root/.engramd
      - ../testnet-data/engram-node01/config/genesis.json:/root/.engramd/config/genesis.json:ro
    command:
      - |
        engramd init attacker-a2-b1 --home /root/.engramd || true
        TARGET_ID=$$(curl -s "http://$${TARGET_PEER%%:*}:26657/status" | jq -r .result.node_info.id)
        sed -i "s#^persistent_peers *=.*#persistent_peers = \"$${TARGET_ID}@$$TARGET_PEER\"#" /root/.engramd/config/config.toml
        sed -i "s#^addr_book_strict *=.*#addr_book_strict = false#" /root/.engramd/config/config.toml
        sed -i "s#^allow_duplicate_ip *=.*#allow_duplicate_ip = true#" /root/.engramd/config/config.toml
        exec engramd start --home /root/.engramd

  attacker-a2-b2:
    <<: *attacker-a2
    hostname: attacker-a2-b2
    container_name: attacker-a2-b2
    networks:
      attacker-subnet-b:
        ipv4_address: 172.31.0.12
      engram-net: {}
    volumes:
      - ../testnet-data/attacker-a2-b2:/root/.engramd
      - ../testnet-data/engram-node01/config/genesis.json:/root/.engramd/config/genesis.json:ro
    command:
      - |
        engramd init attacker-a2-b2 --home /root/.engramd || true
        TARGET_ID=$$(curl -s "http://$${TARGET_PEER%%:*}:26657/status" | jq -r .result.node_info.id)
        sed -i "s#^persistent_peers *=.*#persistent_peers = \"$${TARGET_ID}@$$TARGET_PEER\"#" /root/.engramd/config/config.toml
        sed -i "s#^addr_book_strict *=.*#addr_book_strict = false#" /root/.engramd/config/config.toml
        sed -i "s#^allow_duplicate_ip *=.*#allow_duplicate_ip = true#" /root/.engramd/config/config.toml
        exec engramd start --home /root/.engramd

  attacker-a2-b3:
    <<: *attacker-a2
    hostname: attacker-a2-b3
    container_name: attacker-a2-b3
    networks:
      attacker-subnet-b:
        ipv4_address: 172.31.0.13
      engram-net: {}
    volumes:
      - ../testnet-data/attacker-a2-b3:/root/.engramd
      - ../testnet-data/engram-node01/config/genesis.json:/root/.engramd/config/genesis.json:ro
    command:
      - |
        engramd init attacker-a2-b3 --home /root/.engramd || true
        TARGET_ID=$$(curl -s "http://$${TARGET_PEER%%:*}:26657/status" | jq -r .result.node_info.id)
        sed -i "s#^persistent_peers *=.*#persistent_peers = \"$${TARGET_ID}@$$TARGET_PEER\"#" /root/.engramd/config/config.toml
        sed -i "s#^addr_book_strict *=.*#addr_book_strict = false#" /root/.engramd/config/config.toml
        sed -i "s#^allow_duplicate_ip *=.*#allow_duplicate_ip = true#" /root/.engramd/config/config.toml
        exec engramd start --home /root/.engramd

  attacker-a2-c1:
    <<: *attacker-a2
    hostname: attacker-a2-c1
    container_name: attacker-a2-c1
    networks:
      attacker-subnet-c:
        ipv4_address: 172.32.0.11
      engram-net: {}
    volumes:
      - ../testnet-data/attacker-a2-c1:/root/.engramd
      - ../testnet-data/engram-node01/config/genesis.json:/root/.engramd/config/genesis.json:ro
    command:
      - |
        engramd init attacker-a2-c1 --home /root/.engramd || true
        TARGET_ID=$$(curl -s "http://$${TARGET_PEER%%:*}:26657/status" | jq -r .result.node_info.id)
        sed -i "s#^persistent_peers *=.*#persistent_peers = \"$${TARGET_ID}@$$TARGET_PEER\"#" /root/.engramd/config/config.toml
        sed -i "s#^addr_book_strict *=.*#addr_book_strict = false#" /root/.engramd/config/config.toml
        sed -i "s#^allow_duplicate_ip *=.*#allow_duplicate_ip = true#" /root/.engramd/config/config.toml
        exec engramd start --home /root/.engramd

  attacker-a2-c2:
    <<: *attacker-a2
    hostname: attacker-a2-c2
    container_name: attacker-a2-c2
    networks:
      attacker-subnet-c:
        ipv4_address: 172.32.0.12
      engram-net: {}
    volumes:
      - ../testnet-data/attacker-a2-c2:/root/.engramd
      - ../testnet-data/engram-node01/config/genesis.json:/root/.engramd/config/genesis.json:ro
    command:
      - |
        engramd init attacker-a2-c2 --home /root/.engramd || true
        TARGET_ID=$$(curl -s "http://$${TARGET_PEER%%:*}:26657/status" | jq -r .result.node_info.id)
        sed -i "s#^persistent_peers *=.*#persistent_peers = \"$${TARGET_ID}@$$TARGET_PEER\"#" /root/.engramd/config/config.toml
        sed -i "s#^addr_book_strict *=.*#addr_book_strict = false#" /root/.engramd/config/config.toml
        sed -i "s#^allow_duplicate_ip *=.*#allow_duplicate_ip = true#" /root/.engramd/config/config.toml
        exec engramd start --home /root/.engramd

  attacker-a2-c3:
    <<: *attacker-a2
    hostname: attacker-a2-c3
    container_name: attacker-a2-c3
    networks:
      attacker-subnet-c:
        ipv4_address: 172.32.0.13
      engram-net: {}
    volumes:
      - ../testnet-data/attacker-a2-c3:/root/.engramd
      - ../testnet-data/engram-node01/config/genesis.json:/root/.engramd/config/genesis.json:ro
    command:
      - |
        engramd init attacker-a2-c3 --home /root/.engramd || true
        TARGET_ID=$$(curl -s "http://$${TARGET_PEER%%:*}:26657/status" | jq -r .result.node_info.id)
        sed -i "s#^persistent_peers *=.*#persistent_peers = \"$${TARGET_ID}@$$TARGET_PEER\"#" /root/.engramd/config/config.toml
        sed -i "s#^addr_book_strict *=.*#addr_book_strict = false#" /root/.engramd/config/config.toml
        sed -i "s#^allow_duplicate_ip *=.*#allow_duplicate_ip = true#" /root/.engramd/config/config.toml
        exec engramd start --home /root/.engramd

  attacker-a2-d1:
    <<: *attacker-a2
    hostname: attacker-a2-d1
    container_name: attacker-a2-d1
    networks:
      attacker-subnet-d:
        ipv4_address: 172.33.0.11
      engram-net: {}
    volumes:
      - ../testnet-data/attacker-a2-d1:/root/.engramd
      - ../testnet-data/engram-node01/config/genesis.json:/root/.engramd/config/genesis.json:ro
    command:
      - |
        engramd init attacker-a2-d1 --home /root/.engramd || true
        TARGET_ID=$$(curl -s "http://$${TARGET_PEER%%:*}:26657/status" | jq -r .result.node_info.id)
        sed -i "s#^persistent_peers *=.*#persistent_peers = \"$${TARGET_ID}@$$TARGET_PEER\"#" /root/.engramd/config/config.toml
        sed -i "s#^addr_book_strict *=.*#addr_book_strict = false#" /root/.engramd/config/config.toml
        sed -i "s#^allow_duplicate_ip *=.*#allow_duplicate_ip = true#" /root/.engramd/config/config.toml
        exec engramd start --home /root/.engramd

  attacker-a2-d2:
    <<: *attacker-a2
    hostname: attacker-a2-d2
    container_name: attacker-a2-d2
    networks:
      attacker-subnet-d:
        ipv4_address: 172.33.0.12
      engram-net: {}
    volumes:
      - ../testnet-data/attacker-a2-d2:/root/.engramd
      - ../testnet-data/engram-node01/config/genesis.json:/root/.engramd/config/genesis.json:ro
    command:
      - |
        engramd init attacker-a2-d2 --home /root/.engramd || true
        TARGET_ID=$$(curl -s "http://$${TARGET_PEER%%:*}:26657/status" | jq -r .result.node_info.id)
        sed -i "s#^persistent_peers *=.*#persistent_peers = \"$${TARGET_ID}@$$TARGET_PEER\"#" /root/.engramd/config/config.toml
        sed -i "s#^addr_book_strict *=.*#addr_book_strict = false#" /root/.engramd/config/config.toml
        sed -i "s#^allow_duplicate_ip *=.*#allow_duplicate_ip = true#" /root/.engramd/config/config.toml
        exec engramd start --home /root/.engramd

  attacker-a2-d3:
    <<: *attacker-a2
    hostname: attacker-a2-d3
    container_name: attacker-a2-d3
    networks:
      attacker-subnet-d:
        ipv4_address: 172.33.0.13
      engram-net: {}
    volumes:
      - ../testnet-data/attacker-a2-d3:/root/.engramd
      - ../testnet-data/engram-node01/config/genesis.json:/root/.engramd/config/genesis.json:ro
    command:
      - |
        engramd init attacker-a2-d3 --home /root/.engramd || true
        TARGET_ID=$$(curl -s "http://$${TARGET_PEER%%:*}:26657/status" | jq -r .result.node_info.id)
        sed -i "s#^persistent_peers *=.*#persistent_peers = \"$${TARGET_ID}@$$TARGET_PEER\"#" /root/.engramd/config/config.toml
        sed -i "s#^addr_book_strict *=.*#addr_book_strict = false#" /root/.engramd/config/config.toml
        sed -i "s#^allow_duplicate_ip *=.*#allow_duplicate_ip = true#" /root/.engramd/config/config.toml
        exec engramd start --home /root/.engramd

# Networks (engram-net, attacker-subnet-a/b/c/d) are declared once in the
# root compose.yml, which owns them -- services join by name; redeclaring
# here would hit "conflicts with imported resource".
